pipeline {
    agent any

    stages {
        stage('Executar testes') {
            steps {
                script {
                    sh'cy.signIn("user1", "User1_123")'
                }
            }
        }
    }
}
